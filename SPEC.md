# SPEC.md — ikari-uta(怒り歌)

<!-- scaffold template v1.22.0 から展開(2026-08-31)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

ホメロス『イーリアス』全 24 巻を、**原文の行番号を保ったまま**日本語で読めるようにする。
底本は Perseus Digital Library の校訂本文(Monro & Allen, Editio Tertia)であり、
本プロジェクトはその行構造を一行も落とさずに保つことを譲らない。

行番号を保つのは体裁の問題ではない。行が保たれていれば、**訳文を検査できる**ようになる。
原文・PD 英訳・和訳の三者が同じ行番号で並ぶので、訳抜け・行ズレ・表記ゆれが機械で検出できる。
読み物としての価値と、検査可能性が、同じ一つの設計判断から出ている。

和訳は Claude による機械翻訳であり、学術的な定訳ではない。本プロジェクトが保証するのは
**「落ちていない・ズレていない・表記が揺れていない」**であって、訳の良し悪しではない。
この区別は画面にも文書にも明示する。

## 2. 機能要求

| ID | 要求 | 優先度 | 導入ループ |
|---|---|---|---|
| F-01 | Perseus canonical-greekLit から原文 XML と Murray 英訳 XML を取得し、ローカルにキャッシュする(再実行時は HTTP アクセスを行わない) | must | loop_001 |
| F-02 | TEI を巻・行に分解し、行番号付きの正準データを作る。`<l n>` の欠落・重複・非単調を例外にして止める | must | loop_001 |
| F-03 | 巻ごとの行数と総行数を**実測して**記録し、以後の回帰基準(行数オラクル)にする | must | loop_001 |
| F-04 | 固有名詞の対訳表を凍結する。ギリシャ語形カタカナに統一し(ラテン語形は採らない)、表外の表記を検出する | must | loop_001 |
| F-05 | 第 1 巻の和訳を作る。**原文 1 行 = 訳 1 行**を厳守する逐語寄りの現代語散文 | must | loop_001 |
| F-06 | リーダー UI。巻の選択、原文 / 英訳 / 和訳の表示切替、行番号の常時表示 | must | loop_001 |
| F-07 | 出典・ライセンス・機械翻訳である旨を全ページに表示する | must | loop_001 |
| F-08 | 未訳の巻を「準備中」と明示し、訳済みの巻と取り違えられないようにする | must | loop_001 |
| F-09 | 人物索引(人物 × 出現行 × 陣営 × 父称)を原文から生成する | must | loop_002 |
| F-10 | 相関図。ノード=人物(陣営属性)、辺=血縁 / 指揮 / 殺害 / 対話 / 肩入れ。**全辺が出典行を持つ** | must | loop_002–003 |
| F-11 | 年表。行番号を主軸、劇中日数を従軸とし、人物ごとに初出・最終言及・死を帯で示す | must | loop_003 |
| F-12 | 第 2 巻の軍勢一覧から陣営と指揮系統を抽出する | must | loop_003 |
| F-13 | 戦死の定型句から殺害辺を抽出する(原文と英訳の二経路一致のみ採用、不一致は判定保留)。**英訳側の分解能は ±5 行の窓**であり、行単位の一致は要求できない(§3.1 実測) | must | loop_003 |
| F-14 | 土井晩翠訳(青空文庫・PD)を巻単位で並置する。行対応は取らない | should | loop_005 以降 |
| F-15 | 全文検索(原文 / 和訳) | should | 未定 |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 課金経路を持たない。サーバ関数ゼロ・cron ゼロ・実行時の外部 API 呼び出しゼロの静的配信のみ | 出荷物にサーバ関数が無いこと、および出荷 JS に外部通信呼び出しが無いことをテストで確認 |
| N-02 | 和訳は**開発時**に生成してコミットする。ビルド工程で LLM を呼ばない | ビルドスクリプトに API 呼び出しが無いことをテストで確認 |
| N-03 | コーパス取得は 1.2 秒以上の間隔を空け、キャッシュを優先する | 取得器のテスト(キャッシュ命中時に HTTP を呼ばない) |
| N-04 | 原文 XML は git 管理外(`data/raw/`)。再取得で再現できること | `.gitignore` と再現手順の実行 |
| N-05 | 底本の CC BY-SA 4.0 を継承し、帰属(Monro & Allen / Perseus Digital Library)を表示する | LICENSE.data の存在と、全 HTML への帰属表示をテストで確認 |
| N-06 | 1 巻あたりの配信 JSON は遅延読込とし、初回表示で全巻を配らない | 出荷物の分割状態をテストで確認 |

## 3.1 底本の実測(2026-08-31)

以下は取得した XML を開いて数えた**実測値**である。見込みではない。

### 行数 — 通説の 15,693 は行数ではない

| 量 | 値 |
|---|---|
| 巻数 | 24 |
| **実在する `<l>` 要素** | **15,687** |
| 巻ごとの行番号最大値の和 | 15,693 |
| 行番号の重複 | 0 件(全巻) |
| 行番号の非単調 | 0 件(全巻) |

差の 6 行は、編者(Monro & Allen)が本文から除いたが**番号だけ残した**行である。

| 巻 | 欠番 |
|---|---|
| 9 | 458, 459, 460, 461 |
| 11 | 543 |
| 14 | 269 |

**設計上の帰結: 行番号は連番ではない。配列の添字と行番号を同一視してはならない。**
第 9 巻は 457 の次が 462 である。この同一視は例外を出さずに全下流(訳の整列・年表・相関図の出典行)を
静かに 4 行ずらすので、正準データは行番号を明示的に保持し、添字から導出しない。

### 英訳(Murray)の分解能 — 行単位では照合できない

`perseus-eng3` は `<l>` 要素を**持たない**。24 の巻 div の下に 425 の card div、
427 の `<p>` があり、原文行との対応は `<milestone unit="line" n="...">` で与えられる。

| 量 | 値 |
|---|---|
| line milestone 総数 | 3,143 |
| アンカー間隔の中央値 | 5 行 |
| 間隔が 5 の件数 | 3,082 / 3,143 |
| 間隔の最大 | 15 行 |

**英訳による照合の分解能は ±5 行の窓である。** 塊単位の脱落・大きな整列ずれは捕まえられるが、
1 行のずれは捕まえられない。この限界を超える主張(行単位の二経路一致)を英訳に負わせてはならない。

## 4. 品質基準

数値はすべてテストにトレースできる粒度で書く。**閾値は実測してから置く**(HC-016)。

- **G-01 行数オラクル** — 正準データの巻ごとの行数・行番号集合が、`data/oracle/line_counts.json`
  (loop_001 で原文 XML から実測して凍結)と**集合として完全一致**する。訳データについても
  同じ検査を行い、訳が原文の行を 1 本も落としていないことを保証する
- **G-02 突合** — 対訳表に載る固有名詞について、原文ギリシャ語の出現行と和訳の出現行が**行単位で一致**する
  (この 2 つは同じ行番号体系の上にあるので、行単位を要求できる)。英訳を第三の経路として使う場合は
  **±5 行の窓での一致**しか要求してはならない(§3.1 実測)。不一致は全件列挙し、握りつぶさない。
  **閾値は loop_002 で実測してから置く**
- **G-03 表記ゆれ** — 和訳中に現れる固有名詞のカタカナ表記が、凍結された対訳表の表記と完全一致する。
  表外表記の検出は 0 件を要求する
- **G-04 文字種** — 日本語本文にキリル文字・ハングル・制御文字が混入しない
  (`harness/text_hygiene.py`)。ギリシャ文字は原文フィールドにのみ許し、和訳フィールドでは禁ずる
- **G-05 出典表示** — 全 HTML に底本の帰属・ライセンス・機械翻訳である旨が含まれる
- **G-06 図の出典**(loop_002) — 相関図の全辺が実在行を指し、その行に両端の名前が実在する。未出典の辺 0 件
- **G-07 外部補完の明示**(loop_002) — 本文に根拠のない辺は視覚的に区別し、件数を画面に出す
- **G-08 帯の両端**(loop_003) — 年表の全帯の初出・最終言及が実在行を指す
- **G-09 死の二経路一致**(loop_003) — 死亡マーカーは原文定型句と英訳表現の両方が立つ行のみ採用。
  保留件数を画面に明示する
- **G-10 神の陰性対照**(loop_003) — 神々に死亡マーカーが 1 件も立たない。1 件でも立てば検出器の欠陥として fail

### 目玉の選定について

目玉候補は 3 つある(形容辞の地図 / 神から人への父称の連続 / 三系統の指揮で命令が拒まれる割合)。
**loop_004 で実測するまで選ばない。** 先に選ぶと、結論に合う測り方を選んでしまう。
実測して弱ければ捨て、捨てた記録を残す。

## 5. スコープ外

- **訳の文学的品質の主張**。韻律の再現、詩形の移植は行わない。逐語寄りの散文に徹する
- **トロイア戦争の史実性・年代**への言及。本編に書かれていない年代軸(戦争 10 年目のどこか)は扱わない
- **『オデュッセイア』および叙事詩環**。本プロジェクトはイーリアス本編のみを対象とする
- **土井晩翠訳と原文の行対応**。晩翠訳は七五調への移植であり原文と行対応しない。並置はするが対応づけはしない
- **異読・校訂注(apparatus criticus)**。Monro & Allen の本文のみを扱う
- **ユーザ入力・投稿・アカウント**。静的配信のみで、サーバ状態を持たない
