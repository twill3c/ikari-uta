# TEST_SPEC.md — ikari-uta

<!-- scaffold template v1.22.0 から展開(2026-08-31) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**する

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない** |
| 外部権威 | 出典と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す |

## オラクルの循環を避ける設計(HC-045)

行数オラクルは、正準データを作るのと**同じ経路で数えてはならない**。同じ DOM 走査で数えた値と
比べても恒等式になり、緑でも何も検査していない。本プロジェクトは経路を 2 本に分ける。

| 経路 | 実装 | 用途 |
|---|---|---|
| **経路 A(DOM)** | `xml.etree.ElementTree` で木を walk する | 正準データ `data/canonical/book-NN.json` の生成 |
| **経路 B(走査)** | 生バイト列を正規表現で線形走査する | オラクル `data/oracle/line_counts.json` の生成 |

2 経路は木構造の解釈を共有しない(A は要素の親子関係を、B はバイト列の出現順序を見る)。
**比べるのは結論だけでなく経路も含める**(HC-065): 巻ごとの行番号**集合**に加え、
**行の出現順序列**を比べる。順序列が一致して初めて「同じ理由で一致した」と言える。

さらに**外部権威**を第 3 の錨に置く: 各巻の行番号最大値の和は 15,693 で、これは
イーリアスの行数として広く流通している数字と一致する。**この一致自体が、欠番 6 行の存在を
裏づける独立した証拠**である(実在行 15,687 ≠ 流通値 15,693)。

**陽性対照を対で置く**(HC-041): 経路だけをずらした壊れたパーサ(欠番を詰めて連番にする)を
用意し、照合がそれを落とすことを確かめる。落とせないなら、この照合は経路を見ていない。

## オラクルの出所

| フィクスチャ / オラクル | 出所 | 性格 |
|---|---|---|
| `data/oracle/line_counts.json` | 底本 XML を経路 B で走査した実測(2026-08-31) | 凍結。以後の回帰基準 |
| 行数 15,693(最大値の和) | 外部権威。イーリアスの行数として流通する値 | 独立した錨 |
| `data/glossary.json` | loop_001 で凍結。ギリシャ語形カタカナ | 凍結。表外表記を fail にする |
| `tests/fixtures/tei_broken_*.xml` | 手作りの壊した入力 | 陽性対照 |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 / N-03 | キャッシュ在りで `fetch_all` を呼ぶ | HTTP 呼び出し 0 件。`cached=True` |
| T-002 | F-01 | 取得記録に URN・ライセンス・帰属・sha256 が入る | 4 項目すべて非空 |
| T-003 | F-02 | 底本を経路 A で解析する | 巻 div が 24。全行が数値の行番号を持つ |
| T-004 | F-02 | 行番号の重複を含む入力 | `ValueError` で止まる(黙って通さない) |
| T-005 | F-02 | 行番号が非単調な入力 | `ValueError` で止まる |
| T-006 | F-03 | 経路 A と経路 B の**行番号集合**が巻ごとに一致 | 全 24 巻で一致 |
| T-007 | F-03 | 経路 A と経路 B の**出現順序列**が巻ごとに一致(HC-065) | 全 24 巻で一致 |
| T-008 | F-03 | 陽性対照: 欠番を詰めて連番にした経路 B の贋物 | 照合が **落とす**(緑にならない) |
| T-009 | F-03 | 外部権威: 巻ごとの行番号最大値の和 | 15,693 |
| T-010 | F-03 | 実在行の総数は最大値の和より小さい | 15,687 < 15,693。差 6 |
| T-011 | F-02 | 欠番が正準データで**保持**される(詰められない) | 9 巻に 458–461 が無く、457 の次が 462 |
| T-012 | F-02 | 正準データは行番号を明示保持し、添字から導出しない | 全巻で `lines[i]["n"]` が添字+1 と一致しない巻が存在する |
| T-013 | F-05 / G-01 | 和訳データの行番号集合が原文と一致 | 訳済みの巻で過不足 0 |
| T-014 | F-05 | 和訳の各行が空でない | 空文字・空白のみ 0 件 |
| T-015 | G-03 | 和訳中のカタカナ固有名詞が対訳表に載る | 表外表記 0 件 |
| T-016 | G-03 | 陽性対照: 表外表記を仕込んだ和訳 | 検査が落とす |
| T-017 | G-04 | 和訳フィールドの字種 | ギリシャ文字・キリル・ハングル・制御文字 0 件 |
| T-018 | G-04 | 陽性対照: 字種を混入させた和訳 | 検査が落とす |
| T-019 | G-04 | 陰性対照: 原文フィールドのギリシャ文字は許される(HC-074) | 誤検出 0 |
| T-020 | N-01 | 出荷物にサーバ関数が無い | `api/` 無し。`vercel.json` に関数定義無し |
| T-021 | N-01 | 出荷 JS に外部通信の呼び出しが無い | fetch 先が相対パスのみ |
| T-022 | N-02 | ビルド経路に LLM API 呼び出しが無い | 該当文字列 0 件 |
| T-023 | G-05 / F-07 | 全出荷 HTML に帰属・ライセンス・機械翻訳の明示 | 3 項目すべて含む |
| T-024 | F-08 | 未訳巻の扱い | 一覧で「準備中」と明示され、訳済みと区別できる |
| T-025 | N-06 | 配信 JSON が巻ごとに分かれる | 単一の全巻ファイルが存在しない |
| T-026 | N-05 | データのライセンス表示 | `LICENSE.data` が存在し CC BY-SA 4.0 を明記 |
